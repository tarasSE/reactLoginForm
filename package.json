{
  "name": "login-form",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "eslint": "^5.5.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "enzyme": "^3.3.0",
    "enzyme-adapter-react-16": "^1.1.1",
    "eslint-plugin-react": "^7.11.1",
    "jsdom": "^11.12.0"
  },
  "engines": {
    "node": "^10.7.0"
  },
  "scripts": {
    "precommit": "yarn fix",
    "start": "yarn fix && react-scripts start",
    "build": "yarn fix && react-scripts build",
    "test": "yarn fix && react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "fix": "eslint --fix ./src"
  }
}
